# Plans

Use this folder for larger implementation plans and closed-task records.

- `active/` for in-progress plan docs
- `archive/<year>/` for completed plan docs
- `history/<year>/` for closed-task history logs
